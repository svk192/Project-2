<ul class="nav justify-content-end" id="logout">
    <li class="nav-item">
        <a class="nav-link active" href="/index">Home</a>
    </li>
    <li class="nav-item">
        <a class="nav-link active" href="/logout">Logout</a>
    </li>
</ul>

<div class="container" id="results">
    <h2>Book Results</h2>
    <p>Click on a book to add it to your list and then vote!</p>
    {{#each books}}
    <div class="card">
        <div class="row no-gutters">
            <div class="col-auto">
                <img src="{{this.volumeInfo.imageLinks.thumbnail}}" class="img-fluid" alt=""
                    style="width:250px;height:300px">
            </div>
            <div class="col">
                <div class="card-block px-2">
                    <h5 class="bookTitle">{{this.volumeInfo.title}}</h5>
                    <h6 class="bookAuthor">{{this.volumeInfo.authors}}</h6>
                    <p class="bookPlot">{{this.volumeInfo.description}}</p>
                    <button class="BookButton btn btn-primary" id="bookAdded" data-idname="{{this.id}}" id="{{this.id}}"
                        data-title="{{this.volumeInfo.title}}"> Add Book!</a>
                </div>
                <div class="d-flex justify-content-end">
                    <button type="button" class="btn btn-secondary">&#128077;</button>
                    <button type="button" class="btn btn-secondary">&#128078;</button>
                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"
        integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1"
        crossorigin="anonymous"></script>
    <script src="js/addbook.js" type="text/javascript"></script>